import { LayoutManager } from "golden-layout";

export class WindowManager {

	constructor(
		private layoutManager:
			LayoutManager
	) {}

	openTool(
		toolId: string
	): void {

		// eventually:
		// add tab to layout
	}
}